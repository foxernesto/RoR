<%= form_with(model: testimonio, local: true) do |form| %>
  <% if testimonio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(testimonio.errors.count, "error") %> prohibited this testimonio from being saved:</h2>

      <ul>
      <% testimonio.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_field :descripcion %>
  </div>

  <div class="field">
    <%= form.label :link %>
    <%= form.text_field :link %>
  </div>

  <div class="field">
    <%= form.label :activo %>
    <%= form.check_box :activo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
